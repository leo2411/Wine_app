<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leo.vino12">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".detaljicvina"
            android:label="Crna Vina"
            android:parentActivityName="com.leo.vino12.RedWine" >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.leo.vino12.RedWine" />></activity>

        <activity android:name=".detaljiBvina"
            android:label="Bijela Vina"
            android:parentActivityName="com.leo.vino12.BijeloVino" >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.leo.vino12.BijeloVino" />></activity>
        <activity
            android:name="com.leo.vino12.RedWine"
            android:label="Crna Vina"

            android:parentActivityName="com.leo.vino12.MainActivity" >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.leo.vino12.MainActivity" />
        </activity>

        <activity
            android:name="com.leo.vino12.BijeloVino"
            android:label="Bijela Vina"

            android:parentActivityName="com.leo.vino12.MainActivity" >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.leo.vino12.MainActivity" />
        </activity>
    </application>

</manifest>
